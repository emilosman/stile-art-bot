<h2>
  <%= @board.title %>
  <%= link_to '[delete]', board_path(@board), method: :delete, data: {confirm: 'Are you sure you want to delete this board?'} %>
  <%= link_to '[save & publish]', share_path(@board.share_id) %>
</h2>

<editor></editor>
