<%= link_to @board.title, @board %>

<dropzone></dropzone>

<div class=row>
  <div class="col">
    <% @board.items.each do |item| %>
      <%= link_to item.image do %>
        <%= image_tag item.image, width: '200px' if item.image.present? %>
      <% end %>
    <% end %>
  </div>
</div>
